/**
 * Default image paths (in public/). Use when a property has no images or image fails to load.
 * Place your own default image in public/default_images/ (e.g. property-placeholder.jpg) and use it here.
 */
export const DEFAULT_PROPERTY_IMAGE = '/default_images/property-placeholder.svg';
